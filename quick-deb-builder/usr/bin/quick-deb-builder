#! /bin/bash

# Author: Gustavo Moraes <gustavosotnas@gmail.com>
#
# This file is subject to the terms and conditions of the GNU General Public
# License. See the file COPYING in the main directory of this archive
# for more details.

DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd) # Pega o diretório no qual o script está localizado (default: "/usr/bin")
cd "$DIR"/../share/quick-deb-builder
sudo ./quick-deb-builder.sh "$HOME" "$USER" # quick-deb-builder.sh (String home, String user) # Como a pasta sudoers.d está modificada, o sudo não irá perguntar senha